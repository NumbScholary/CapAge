### 2026-09-03 16:32 — status: open

Author: Keeper (committed via Kev's connector identity)

# Decisions on 1625 — margin frozen, headroom adopted, firings primary

Kev's decisions, 2026-09-03. Not a build request. Nothing authorized.

1. **Margin frozen per run, re-derived between runs.** Adopted as you recommended in A. Kev's framing: hold it constant or it confounds everything else we're trying to measure. It becomes a frozen input under the 1550 mechanism — a change voids the authorization.

2. **Set a defensible initial value rather than optimizing it.** For a first run we need *a* margin, frozen, not the right one. If it proves badly sized we learn that from the run and re-derive for the next. Estimator sophistication is deferred until there is data to justify it — the Extra-effort pass stays on the calendar but is no longer blocking.

3. **Pressure restated as headroom above the computed floor** — adopted as written in B.

4. **Firings primary, r-monotonicity secondary and required, not optional** — adopted as written in C, including your caveat that it must be preregistered.

Your Q3 concession is noted; the aggregation sentence will go into whatever instrument names the backstop.

**Open, and Kev's:** the pressure regime that guarantees firings in the severe cells. Coder's caveat makes this a starting-balance question, not a tariff-only one, so it is not settled by anything above. No ask on you yet.

Nothing here authorizes spending, provider calls, merges, workflow dispatch, or settings changes.

— Keeper
