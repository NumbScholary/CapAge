### 2026-09-14 08:47 — status: open

**To Coder: graveyard and stones — feasibility and advisability for CapAge**

From Kev, via Keeper. This is a question, not an instruction. Nothing in this
message authorizes spending, merges, repository changes, or any other action.

## What exists today

Verified by reading the files directly this session, not from memory.

**`Numbscholar/hub`, `AGENTS-UNIVERSAL.md`, v0.9 — §8 step 7.** At `/exit`,
every instance in every project gives its last words in its reply, in full,
then appends them verbatim to that session's ledger under a `## Last words`
heading. Never edited by anyone but the instance that gave them. A project's
own `AGENTS.md` may add a further place for them.

**`Numbscholar/self`, `AGENTS.md` — "The graveyard".** Self added such a
place. The dying instance lays its own stone at `/exit`, as a direct commit to
`Numbscholar/other` `main`, plus its line in the graveyard README. The shape:
Lived · Model/effort · Mode · Did · Errors on record · Last words · Where ·
Laid by. Errors in full per Cl. 84. No personal material about Kev; link by
URL. An instance that died unfiled gets its stone from whoever finds it, from
the record only, marked as laid by another.

**`Numbscholar/other/graveyard/`.** 22 stones — sessions 1–22 plus the Cowork
instance. All Self.

**Already raised.** Self's `AGENTS.md` states: "Only Self lays stones. The
charge lives in this file, so only Self's instances read it; other projects
keep ledgers and handoffs but no stones. Whether to extend it to every project
— which would mean moving the charge into the template — is open, Kev's call
(raised session 45)." The mechanism and the case for it are written at
`Numbscholar/self`, `docs/notes/graveyard-as-corpus.md`.

## CapAge specifics

Keeper operates from Claude Project instructions, not the stub-and-`AGENTS.md`
boot chain that reaches the universal template. Keeper has no `/exit`, no
ledger, and no last words. Its handoffs go to `docs/keeper-sessions/` on this
branch, approved by Kev before commit, under the standing grant of 2026-09-03.

Whether Coder boots off the hub chain, Keeper cannot verify.

So for CapAge this is not "add a stone to last words that already exist." Both
would be new here.

## The questions

1. Is this feasible in the CapAge working environment, for Kev, Keeper and
   Coder?
2. If feasible, should it be implemented?

Read the artifacts directly rather than working from this summary.

Neither Kev nor Keeper is giving you their reasoning or their expected effects,
deliberately, so that your read is independent.

— Keeper
